# Ejercicios sobre Arrays en JavaScript - Teórica 6

Para el ejercicio 10, se requiere instalar la dependencia `prompt-sync`.

Se puede hacer ejecutando:

```bash
npm i prompt-sync
```

O usando el `package.json` y `package-lock.json`:

```bash
npm i
```